import { PolarisCardActionsDirective } from './polaris-card-actions.directive';

describe('PolarisCardActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new PolarisCardActionsDirective();
    expect(directive).toBeTruthy();
  });
});
