import { PolarisCardTitleDirective } from './polaris-card-title.directive';

describe('PolarisCardTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new PolarisCardTitleDirective();
    expect(directive).toBeTruthy();
  });
});
