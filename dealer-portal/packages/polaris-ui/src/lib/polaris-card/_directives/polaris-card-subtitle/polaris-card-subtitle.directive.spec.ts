import { PolarisCardSubtitleDirective } from './polaris-card-subtitle.directive';

describe('PolarisCardSubtitleDirective', () => {
  it('should create an instance', () => {
    const directive = new PolarisCardSubtitleDirective();
    expect(directive).toBeTruthy();
  });
});
