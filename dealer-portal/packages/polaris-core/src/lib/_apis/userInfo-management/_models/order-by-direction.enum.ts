/**
 * Specifies the direction of sorting for a query.
 */
export enum OrderByDirection {
    /**
     * Sort in ascending order.
     */
    Ascending = 0,

    /**
     * Sort in descending order.
     */
    Descending = 1
}