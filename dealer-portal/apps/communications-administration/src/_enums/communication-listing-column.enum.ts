// Enum to have easily identifiable columns to sort by for the the Communication Admin Listing table
export enum CommunicationListingColumn {
  Title = 'title',
  StartDate = 'startDate',
  EndDate = 'endDate',
  Group = 'group',
  SubGroup = 'subGroup',
  CreatedOn = 'createdOn',
  CreatedBy = 'createdBy',
  Status = 'status',
  Translations = 'translations',
  Actions = 'actions',
}
