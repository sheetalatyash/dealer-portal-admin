export enum Route {
  Home = '',
  CommunicationDetails = 'details/:communicationGuid',
  ToggleAccount = 'toggle-account',
  EditWidgets = 'edit-widgets',
  PolarisConnect = 'polaris-connect/:communicationGuid',
}
